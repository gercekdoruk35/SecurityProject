services:
  mongodb:
    image: mongo:7
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db

  scanner:
    build: ./scanner
    depends_on:
      - mongodb

volumes:
  mongo_data:
